# sk-enterprise (e-bill)

GST-style invoice PDF app (Next.js). Run locally with `npm install` and `npm run dev`.

Deploy on [Vercel](https://vercel.com): set **`BLOB_READ_WRITE_TOKEN`** in project env (Vercel Blob) so profile, drafts, and saved bills persist.
